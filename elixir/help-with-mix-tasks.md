# Getting help with mix tasks

It's very useful to check documentation for mix tasks using the `mix help`:

```
$ mix help phoenix.gen.html
```

It shows the contents of `@moduledoc` from the given mix task.
